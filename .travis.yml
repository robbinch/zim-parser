# Use new container infrastructure to enable caching
sudo: false

# Choose a lightweight base image; we provide our own build tools.
language: c

# GHC depends on GMP. You can add other dependencies here as well.
addons:
  apt:
    packages:
    - libgmp-dev
    - liblzma-dev

# The different configurations we want to test. You could also do things like
# change flags or use --stack-yaml to point to a different file.
env:
- ARGS="" CMD="test"
# lts-2 cannot build test suite
- ARGS="--resolver lts-2" CMD="build"
- ARGS="--resolver lts-3" CMD="test"
- ARGS="--resolver lts" CMD="test"
- ARGS="--resolver nightly" CMD="test"

before_install:
# Download and unpack the stack executable
- mkdir -p ~/.local/bin
- export PATH=$HOME/.local/bin:$PATH
- travis_retry curl -L https://github.com/commercialhaskell/stack/releases/download/v0.1.4.0/stack-0.1.4.0-x86_64-linux.tar.gz | tar xz -C ~/.local/bin

# This line does all of the work: installs GHC if necessary, build the library,
# executables, and test suites, and runs the test suites. --no-terminal works
# around some quirks in Travis's terminal implementation.
script:
- stack init
- stack $ARGS --no-terminal --install-ghc $CMD --haddock

# Caching so the next build will be fast too.
cache:
  directories:
  - $HOME/.stack